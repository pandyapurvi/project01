<h1>Brands</h1>
<% @brands.each do |brand| %>
<% if brand.image %>
<%= image_tag brand.image, :class => 'thumb' %>
<% end %>
<ul>
  <li>Name: <%= link_to brand.name, brand_path(brand.id) %></li>
</ul>
<% end %>
